# EXP-20260821-034 — EXP-033 Input Canonicalization Proposal v0.2

## Status

- Lifecycle: `PROPOSED` locally; registry event is not created in this proposal-only step
- Score: `75/100`
- Automatic execution: `false`
- Prepare approval: **not granted**
- Run approval: **not granted**
- This Issue requests review of the proposal only.
- Real rows opened/materialized: `0`
- Model fit, validation, calibration, outer OOS, prediction: `0`

## Canonical authority

- Experiment ID: `EXP-20260821-034`
- Single variant: `exp033_input_canonicalization_v0`
- Proposal: `research/scopes/EXP-20260821-034.proposal.json`
- Proposal digest: `da76a639a01a179f3061adaa14236741c226b223772953bb5551b2d277351021`
- Input contract: `research/drafts/EXP-20260821-034.input_canonicalization_contract.json`
- Input contract SHA-256: `1d72ca2443f3bb49c912074152c5afafb0b6e9e50647ab7b571cb7a072caf21a`
- Fold manifest SHA-256: `b939dd6504e59d2214324141726bb0b0e91c7c762c99bb508743fee7257737e1`
- Synthetic fixture plan SHA-256: `f79e322785063cb41fa610a7f4ee7f904e291faf5cf7a78033edf64fbc32668c`
- Run scope/digest: `pending`
- Base branch: `main`
- Base commit/current main at proposal audit: `0f3d2cfa8081f76bbb6d890b98abce67f3705e1a`
- `research/APPROVERS.json` Git blob:
  `c973f7d83de78cc0eea09ef6a240e99e4512937e`
- `research/APPROVERS.json` content SHA-256:
  `ac14971c8d8f6f4502c30c6b6434da9ce061009955461248c95363b36ee137b5`

The GitHub evidence above is read-only and is not an approval grant.

## Pre-registration revision

- Revision reason: `PRE_REGISTRATION_LINE_ENDING_NORMALIZATION`
- Superseded unpublished proposal digest:
  `4fd122e8a5e061a926594a4f0eae2bdf7fcc3556dbda3b2fc7a650fa9c81a0d6`
- Superseded Prepare comment ID: `5369470685`
- The unpublished PROPOSED event `75208249-ebea-492d-b9a0-f9107e465c9f`
  was discarded and is not part of canonical Registry history.
- The old comment remains unedited and undeleted on Issue #49, but it must never
  be reused as approval evidence.
- The fold manifest and input contract are now strict canonical JSON bytes
  (UTF-8, sorted keys, compact separators, no BOM, one terminal LF). The
  synthetic fixture plan is UTF-8, no BOM, LF-only.
- Domain semantics, score, schema, population, feature contract, gates, and
  safety flags are unchanged. Only deterministic serialization and dependent
  hashes/digest were revised before registration.

## Hypothesis / null

A deterministic, default-deny canonicalizer can turn the frozen metadata-bound
JRA/history/Recovery sources into immutable training/target generator-input,
runner-universe, lineage, eligibility, and hash manifests that EXP-033 can bind,
without target result, future, draw-derived, or market information.

The null is retained and no certified release is emitted if any identity, status,
source time, receipt, schema, missing reason, label reconciliation, or repeat hash
cannot be proven exactly.

## Frozen population and schemas

- Predraw baseline: five races, 70 declared `(race_id, horse_id)` identities.
- Expected audited states: 44 `scheduled_pending_draw`, 26 `confirmed`.
- Pending draw numbers remain null; provisional and zero numbers are forbidden.
- Horse names are display-only; name joins are forbidden.
- Declared, active-as-of, and supervised-label universes remain separate.
- EXP-033 compatibility is fixed to the existing ordered 88-feature contract:
  77 numeric plus 11 categorical. EXP-034 does not create an alternative model or
  modify EXP-033.

Every source record binds `source_event_time`, `received_at`, `available_as_of`,
`source_version`, source content SHA-256, and a defined `missing_reason`.
Filesystem mtime or generation time cannot substitute for receipt evidence.

## Label eligibility

All declared identities and official statuses remain in an append-only audit
ledger. A historical race is eligible for EXP-033 labels only when every effective
starter has a complete official result and the integer ranks are exactly the unique
set `1..n`.

- Pre-cutoff scratches/nonstarters are retained declared but excluded active.
- DNF, disqualification, abnormal/void, partial result, dead heat, missing rank,
  duplicate rank, or rank gap makes the whole race label-ineligible.
- No row-only filtering, imputation, compaction, or invented tie order is allowed.
- The 2026-08-23 target contains no result records.

## Immutable versions and Saturday refresh

`predraw_v1` freezes the 70 declared keys. A later child version may update only
official draw/status, scratch, jockey, carried-weight, lineage, and diff fields.
Draw-only changes must not alter the 88-feature payload. Scratch, jockey, or weight
changes regenerate only their dependent target artifacts. Historical training
release, EXP-033 schema, folds, and label policy remain immutable.

Unknown Saturday bytes cannot be inserted into an approved scope. A child version
must have its own bound input hash and approval. If a scratch makes EXP-033's frozen
70/70 inference gate unsatisfiable, EXP-033 needs a new approved scope; this proposal
does not reinterpret that gate.

## Sources — metadata references only

- `date/raw/全競走馬成績.csv` —
  `5e7e88ec69b313b9a9d30af41394b5b40374542e0d42c29f18a377bc9bbb7c0f`
- Recovery runner snapshot —
  `4ec29eea0f35d478161b6472ca325f5cc08399cffa71d8e73a196bc7e12dc661`
- Recovery target history —
  `605587d525dfb5c6aecc720b8f97f82b42edf7c382976403d2e9d7cb5c86cb66`
- T0 lineage audit —
  `a7dd4050aea75e28fa1483c9a9c4e11ec42285578d23fc5812e57ddca8a48283`
- EXP-033 proposal digest —
  `6993e9f5a6e0d6b2ef726bbc65fd047479a7b1bf79e948689a588f26f034ff6d`
- EXP-033 allowlist / denylist / fold hashes are frozen in the canonical scope.
- Any later official acquisition is JRA-only; netkeiba is forbidden.

No source rows were opened to write this proposal.

## Scorecard

| Component | Score | Maximum |
|---|---:|---:|
| Independent information | 8 | 25 |
| Racing mechanism | 18 | 20 |
| Outer-OOS failure evidence | 15 | 20 |
| Leakage safety | 15 | 15 |
| Minimal falsifiability | 10 | 10 |
| Acquisition / implementation cost | 9 | 10 |
| **Total** | **75** | **100** |

The score is threshold-edge and claims no new predictive performance. It is based
on provenance, train/live compatibility, and falsifiable fail-close checks.

## Governance blocker

Prepare approval permits implementation and synthetic fixtures only; it does not
permit real-row materialization. Current ordinary schema-v2 Research OS rejects a
real-data `RUNNING` transition because its run scope does not bind
`execution_kind`. A human-reviewed versioned ordinary real-data run contract must
be merged before EXP-034 can materialize any canonical release.

Other unresolved facts are authoritative receipt/source times in legacy inputs,
complete target card codes, historical declared-card versus result reconciliation,
and the unknown Saturday input hash. Each fails closed if it cannot be proven.

## Absolute safety

- `formal_buy=false`
- `send_order=false`
- `stake=0`
- No Champion, production, BUY, notification, order, or stake change
- No training, validation, calibration, outer OOS, prediction, ROI, or target result
- No netkeiba
- No automatic merge or promotion

## Exact Prepare approval comment

Only an authorized human may post this exact Issue comment:

```text
APPROVED_TO_PREPARE da76a639a01a179f3061adaa14236741c226b223772953bb5551b2d277351021
```

This grants only Prepare implementation and synthetic tests after the formal
lifecycle transitions. Real-data materialization still requires a distinct,
input-bound `APPROVED_TO_RUN` and a Research OS contract capable of authorizing
`execution_kind=real-data`.
