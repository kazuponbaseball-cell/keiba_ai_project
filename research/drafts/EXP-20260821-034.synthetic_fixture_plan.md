# EXP-20260821-034 Synthetic Fixture Plan

This file freezes proposal-time tests only. No fixture may read a real-data path.
Execution requires a verified `APPROVED_TO_PREPARE` lifecycle; real-row
materialization requires a later, distinct real-data run authorization.

## Fixed fixture families

1. Five synthetic races and 70 synthetic runners, with 44
   `scheduled_pending_draw` rows whose frame and horse numbers are null.
2. Identity failures: duplicate `(race_id, horse_id)`, blank identifiers,
   horse-name join, provisional zero numbers, and unexplained additions.
3. Source-time boundaries for card, completed result, later-history use,
   timezone awareness, and race/event-time atomic updates.
4. Canonical source payload mutation with unchanged hash, malformed hash,
   blank source version, missing receipt evidence, and filesystem-mtime
   substitution.
5. Missing-reason consistency and structural no-history behavior.
6. Complete rank permutation, scratch, nonstarter, DNF, disqualification,
   abnormal/void race, dead heat, and rank-gap reconciliation. No synthetic
   test may invent or compact official ranks.
7. Exact ordered 88-feature schema and dtype parity against the frozen
   EXP-033 allowlist; unknown generated columns and non-finite values fail.
8. Draw, odds, popularity, market, payout, ROI, target result, and future-row
   mutation isolation.
9. Per-feature dependency hashes, including all runners used by a declared-field
   aggregate, and deterministic canonical serialization.
10. Immutable v1 to v2 draw/scratch/jockey change, parent digest, allowed-field
    diff, unexplained-change rejection, and no mutable `latest` alias.
11. Root manifest counts, all artifact hashes, safety flags, and tamper failure.
12. Static prohibition of model fitting, validation, calibration, OOS access,
    prediction, Champion/BUY/order/notification imports, netkeiba, and external
    writes.

## Required Prepare result

- All synthetic tests pass on Python 3.11 and 3.12.
- Real-data rows opened: `0`.
- Network calls: `0`.
- `formal_buy=false`, `send_order=false`, `stake=0`.
- No canonical real release, model artifact, prediction, or performance metric is
  emitted during Prepare.
